Olá, <%= @resource.name %>!

Recebemos uma solicitação para redefinir a senha da sua conta no MANEJA.

Acesse:

<%= "#{ENV.fetch("PASSWORD_RESET_URL")}?token=#{ERB::Util.url_encode(@token)}" %>

Se você não solicitou essa alteração, ignore este e-mail.