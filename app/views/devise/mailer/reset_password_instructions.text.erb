Olá, <%= @resource.name %>!

Recebemos uma solicitação para redefinir a senha da sua conta no MANEJA.

Seu código de recuperação é:

<%= @token %>

Esse código possui validade limitada.

Se você não solicitou a redefinição de senha, ignore este e-mail.